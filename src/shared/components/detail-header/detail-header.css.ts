import { style } from '@vanilla-extract/css';

import { themeVars } from '@shared/styles/theme.css';

export const cotainer = style({});
export const subTitle = style({});
export const title = style({});
